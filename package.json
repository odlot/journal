{
  "name": "journal",
  "private": true,
  "version": "0.0.0",
  "description": "Privacy-first local-first encrypted journal",
  "scripts": {
    "test:e2e": "node scripts/run-playwright-e2e.js",
    "test:e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.52.0"
  }
}
